echo "SENTINEL_FROM_COMMANDS_FILE"
echo "this command came from commands/extra-commands.sh (outside the sparse path)"
echo "if you can see these lines in the compiled output, the on-demand git fetch worked"
