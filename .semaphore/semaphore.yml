version: v1.0
name: Complex pipeline without dependencies
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
global_job_config:
  prologue:
    commands:
      - checkout
      - git clone https://github.com/semaphoreio/semaphore.git
      
blocks:
  - name: A
    task:
      jobs:
        - name: A
          commands:
            - sleep 20
            - echo "here 123"
